#Definición de función por parametros, al finalizar debemos llamarla con dos parametros (los dos señalados (letra y veces))
#Imprimimos por pantalla el segundo parametro ingresado por el usuario, e imprimimos el primer parametro multiplicado por
#el segundo parametro.
def generar_n_caracteres(letra,veces):
	print(veces)
	print(letra*veces)
generar_n_caracteres("a",5)